# Broccoli & Co. Launch Web App

## Getting Started

1. Run `yarn install`

2. Run `yarn run dev`

### To build a production build, run the following command:

Run `yarn run build`

### To run unit tests
